% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RaCInG_input_generation.R
\name{prepare_input_files}
\alias{prepare_input_files}
\title{Build RaCInG input files from raw count data}
\usage{
prepare_input_files(
  counts,
  output_folder = "Results/",
  deconv = NULL,
  cc_network = NULL,
  fun_LR = min,
  cell_expr_profile = NULL,
  source = "source_genesymbol",
  target = "target_genesymbol",
  deconv_method = "Quantiseq",
  cbsx.name = NULL,
  cbsx.token = NULL,
  file_name = NULL,
  signed = FALSE,
  already_normalized = FALSE,
  expr_threshold = 10
)
}
\arguments{
\item{counts}{Gene-by-sample count matrix.}

\item{output_folder}{Directory where the generated \code{L}, \code{R}, \code{C}, and \code{LR} files are written.}

\item{deconv}{Optional patient-by-cell-type abundance matrix. If omitted, it
is computed via \code{multideconv::compute.deconvolution()} followed by
\code{multideconv::compute.deconvolution.analysis()} (which identifies and
collapses correlated cell-type subgroups) and
\code{multideconv::standardize_celltype_colnames()}.}

\item{cc_network}{Optional ligand-receptor prior network. If omitted, it is
retrieved via \code{liana::get_curated_omni()} (a curated, OmniPath-backed
consensus of CellPhoneDB/CellChatDB/ICELLNET/connectomeDB2020/CellTalkDB).}

\item{fun_LR}{Function used to combine ligand and receptor expression values.}

\item{cell_expr_profile}{Optional gene-by-cell-type expression profile
matrix. If omitted, it is estimated from \code{counts} and \code{deconv} via
per-gene non-negative least squares (see \code{.estimate_expression_profiles()}).}

\item{source, target}{Column names to use as ligand and receptor identifiers when \code{cc_network} is supplied.}

\item{deconv_method}{Deconvolution method(s) passed to \code{multideconv::compute.deconvolution()}.}

\item{cbsx.name, cbsx.token}{Optional credentials forwarded to the deconvolution workflow.}

\item{file_name}{File stem used when exporting the generated CSV files.}

\item{signed}{Logical; if \code{TRUE}, also try to load a sign matrix from \code{output_folder}.}

\item{already_normalized}{Logical; if \code{TRUE}, \code{counts} is treated as
already TPM-normalized (linear scale, not logged) and the internal
\code{ADImpute::NormalizeTPM()} gene-length/library-size normalization is
skipped. Use this for datasets where only pre-normalized expression is
available (e.g. public cohorts distributed as log2(TPM+1) matrices with
no raw counts) -- applying \code{NormalizeTPM()} to already-normalized data
would re-apply its gene-length correction on top of the original
normalization, distorting relative gene expression levels.}

\item{expr_threshold}{Minimum expression value (in \code{cell_expr_profile}
units) a ligand or receptor must reach in its sending/receiving cell type
for that ligand-receptor-celltype triple to be kept in \code{CC_table}.}
}
\value{
A named list with the processed input matrices and their labels:
\code{Lmatrix}, \code{Rmatrix}, \code{Cmatrix} (normalised), \code{LRmatrix} (3-D tensor),
\code{celltypes}, \code{ligands}, \code{receptors}, \code{Sign_matrix}, and \code{CC_table}.
}
\description{
This function combines the preprocessing and input-loading steps into a single
call. It generates the \code{L}, \code{R}, \code{C}, and \code{LR} CSV files from raw counts,
then reads them back to produce the normalised matrices and 3-D tensor required
by the kernel and Monte Carlo workflows.
}
