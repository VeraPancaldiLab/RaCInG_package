% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistical_analysis.R
\name{correlate_features_with_score}
\alias{correlate_features_with_score}
\title{Correlate network features with an external score}
\usage{
correlate_features_with_score(
  feature_matrix,
  score,
  group = NULL,
  p_adjust_method = "fdr"
)
}
\arguments{
\item{feature_matrix}{Numeric matrix or data frame with patients in rows
(identified by \code{rownames()}) and features in columns.}

\item{score}{Named numeric vector of per-patient scores. Names must match
\code{rownames(feature_matrix)}; patients missing a score are dropped.}

\item{group}{Optional grouping vector (e.g. cancer type or cohort), in the
same row order as \code{feature_matrix}. When supplied, correlations are also
computed separately within each group.}

\item{p_adjust_method}{Multiple-testing correction method passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}.}
}
\value{
A data frame with \code{Group}, \code{Feature}, \code{Rho}, \code{P_value}, and \code{Adjusted_P_value}.
}
\description{
Computes the Spearman correlation between an external per-patient score
(e.g. an immune response score) and every feature column, pooled across
all patients ("All") and, optionally, within each group separately.
}
