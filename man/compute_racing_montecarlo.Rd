% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Monte_Carlo_Method.R
\name{compute_racing_montecarlo}
\alias{compute_racing_montecarlo}
\title{Run the full Monte Carlo RaCInG workflow}
\usage{
compute_racing_montecarlo(
  counts = NULL,
  output_folder = "~/Documents/racing/vignettes/",
  deconv = NULL,
  cc_network = NULL,
  fun_LR = min,
  cell_expr_profile = NULL,
  source = "source_genesymbol",
  target = "target_genesymbol",
  signed = FALSE,
  deconv_method = "Quantiseq",
  cbsx.name = NULL,
  cbsx.token = NULL,
  pt_idx = NULL,
  file_name = NULL,
  nPatients = "all",
  communication_type = "W",
  Ncells = 10000,
  Ngraphs = 100,
  Ndegree = 20,
  remove_direction = TRUE,
  norm = FALSE,
  input_data = NULL,
  ncores = 1
)
}
\arguments{
\item{counts}{Gene-by-sample count matrix. Required when \code{input_data} is not
supplied; ignored otherwise.}

\item{output_folder}{Directory used to write intermediate and output files.}

\item{deconv}{Optional patient-by-cell-type abundance matrix. If omitted, it
is computed via \code{multideconv::compute.deconvolution()} followed by
\code{multideconv::compute.deconvolution.analysis()} (which identifies and
collapses correlated cell-type subgroups) and
\code{multideconv::standardize_celltype_colnames()}. See \code{\link[=prepare_input_files]{prepare_input_files()}}.}

\item{cc_network}{Optional ligand-receptor prior network. If omitted, it is
retrieved via \code{liana::get_curated_omni()}. See \code{\link[=prepare_input_files]{prepare_input_files()}}.}

\item{fun_LR}{Function used to combine ligand and receptor expression values.}

\item{cell_expr_profile}{Optional gene-by-cell-type expression profile
matrix. If omitted, it is estimated from \code{counts} and \code{deconv} via
per-gene non-negative least squares. See \code{\link[=prepare_input_files]{prepare_input_files()}}.}

\item{source, target}{Column names to use as ligand and receptor identifiers in \code{cc_network}.}

\item{signed}{Logical; if \code{TRUE}, also try to load a sign matrix.}

\item{deconv_method}{Deconvolution method(s) used when \code{deconv} is not supplied.}

\item{cbsx.name, cbsx.token}{Optional credentials for the deconvolution workflow.}

\item{pt_idx}{Optional single patient index to simulate.}

\item{file_name}{File stem used for intermediate files.}

\item{nPatients}{Number of patients to process, or \code{"all"}.}

\item{communication_type}{Feature family to simulate: \code{"D"}, \code{"W"},
\code{"TT"}, \code{"CT"}, \code{"GSCC"}, or a character vector of several of these
(e.g. \code{c("D", "W", "TT", "CT", "GSCC")}). When more than one is given,
every requested type is extracted from the same simulated graphs (via
\code{\link[=countAllTypes]{countAllTypes()}}) in one pass instead of re-simulating a fresh set of
graphs per type -- graph generation, not feature extraction, is the
expensive part of a Monte Carlo run, so this amortizes that cost across
every type requested. \code{output} in the return value is then a named list
(one entry per type) instead of a single result.}

\item{Ncells}{Number of cells per simulated graph.}

\item{Ngraphs}{Number of Monte Carlo iterations.}

\item{Ndegree}{Target average degree.}

\item{remove_direction}{Logical; if \code{TRUE}, merge directionally equivalent features.}

\item{norm}{Logical; if \code{TRUE}, also run a uniformized baseline simulation and
express features as an enrichment ratio over it (isolates specificity from
abundance/topology, at the cost of a second, noisier simulation pass -- budget
a larger \code{Ngraphs} if enabling this). Default \code{FALSE} returns the raw,
abundance-weighted communication magnitude, which is simpler, cheaper (one
pass), and less noisy at a given \code{Ngraphs}.}

\item{input_data}{Optional named list of pre-computed input matrices as returned
by \code{\link[=prepare_input_files]{prepare_input_files()}}.
Must contain \code{Lmatrix}, \code{Rmatrix}, \code{Cmatrix}, \code{LRmatrix}, \code{celltypes},
\code{ligands}, and \code{receptors}.
When supplied, the \code{counts} argument and all preprocessing parameters
(\code{deconv}, \code{cc_network}, etc.) are ignored.}

\item{ncores}{Number of cores to compute patients on in parallel. Passed
through to \code{\link[=runSim]{runSim()}}; see its documentation for details.}
}
\value{
A list with the generated inputs and processed feature matrices.
}
\description{
Run the full Monte Carlo RaCInG workflow
}
