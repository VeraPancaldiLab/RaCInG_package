% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Monte_Carlo_Method.R
\name{.mean_sd_last_dim}
\alias{.mean_sd_last_dim}
\title{Mean and SD across the last dimension of an array, vectorized}
\usage{
.mean_sd_last_dim(arr)
}
\arguments{
\item{arr}{Array whose last dimension is averaged over.}
}
\value{
A list with \code{mean} and \code{sd}, each an array with one fewer
dimension than \code{arr} (the last dimension collapsed).
}
\description{
Equivalent to \code{apply(arr, seq_len(length(dim(arr)) - 1), mean)} /
\code{apply(arr, ..., stats::sd)}, but avoids calling \code{mean()}/\code{sd()} once per
element of the other dimensions -- for a \verb{[264,264,264,itNo]} tensor that's
~18.4 million individual R function calls per statistic with \code{apply()},
which takes well over a minute regardless of \code{itNo}. Reshaping to a matrix
and using \code{rowMeans()}/a vectorized variance formula does the same
computation in a fraction of a second.
}
\keyword{internal}
