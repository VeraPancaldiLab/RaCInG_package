% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Kernel_Method.R
\name{computeTriangles}
\alias{computeTriangles}
\title{Compute triangle features from kernel matrices}
\usage{
computeTriangles(
  kernel,
  cell_names,
  patient_names,
  Dcell = NULL,
  unifKernel = NULL,
  norm = FALSE,
  bundle = TRUE
)
}
\arguments{
\item{kernel}{Kernel array from \code{\link[=compute_kernel]{compute_kernel()}}.}

\item{cell_names}{Character vector of cell-type names.}

\item{patient_names}{Character vector of patient names.}

\item{Dcell}{Patient-by-cell-type abundance matrix, used to weight raw
(unnormalized) scores. Ignored when \code{unifKernel}/\code{norm} is used.}

\item{unifKernel}{Optional normalized baseline kernel.}

\item{norm}{Logical; if \code{TRUE}, divide by the baseline triangle scores.}

\item{bundle}{Logical; if \code{TRUE}, aggregate all directions into a single
"Tr" triangle score per triple. If \code{FALSE}, two scores are returned per
triple: "TT" (trust/transitive triangle, \code{i->j->k} and \code{i->k}) and,
for \verb{i<=j<=k} only, "CT" (cycle triangle, \code{i->j->k->i}).}
}
\value{
A patient-by-feature data frame of triangle scores. Triples with
no possible ligand-receptor pathway in any patient (zero for every
patient) are dropped rather than returned as all-zero columns.
}
\description{
Compute triangle features from kernel matrices
}
