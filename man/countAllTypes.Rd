% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Monte_Carlo_Method.R
\name{countAllTypes}
\alias{countAllTypes}
\title{Extract multiple communication feature types from the same simulated graphs}
\usage{
countAllTypes(
  Dcell,
  Dconn,
  lig,
  rec,
  cellnames,
  N,
  av,
  itNo,
  types = c("D", "W", "TT", "CT", "GSCC")
)
}
\arguments{
\item{Dcell}{Cell-type abundance vector for one patient.}

\item{Dconn}{Ligand-receptor probability matrix for one patient.}

\item{lig}{Cell-by-ligand compatibility matrix.}

\item{rec}{Cell-by-receptor compatibility matrix.}

\item{cellnames}{Character vector of cell-type names.}

\item{N}{Number of cells per simulated graph.}

\item{av}{Target average degree.}

\item{itNo}{Number of Monte Carlo iterations.}

\item{types}{Character vector of feature families to extract from each
simulated graph: any combination of \code{"D"}, \code{"W"}, \code{"TT"}, \code{"CT"}, \code{"GSCC"}.}
}
\value{
A named list, one element per requested entry in \code{types}, each
with the same structure the corresponding single-type \verb{count*()}
function returns (e.g. \code{out$D} matches \code{\link[=countDirect]{countDirect()}}'s return value).
}
\description{
Generates the requested number of Monte Carlo graph realizations \emph{once}
per patient and extracts every requested feature family from each
realization, instead of the alternative of calling \code{countDirect()},
\code{countWedges()}, etc. separately -- which would independently re-simulate
a brand new set of random graphs per feature type. Graph generation
(\code{model1()}'s vertex/edge sampling), not feature extraction, is the
expensive part of a Monte Carlo run, and it does not depend on which
feature you eventually want -- so sharing one set of realizations across
every requested type avoids paying that cost once per type.
}
